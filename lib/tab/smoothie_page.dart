import 'package:flutter/material.dart';

class Smoothie extends StatelessWidget {
  const Smoothie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
